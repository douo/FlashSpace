# FlashSpace Project Instructions

- For upstream merge, personal release packaging, ad-hoc signing, or install tasks, use the project-local skill at `.codex/skills/flashspace-local-release`.
- Do not add CLI codesign commands to `project.yml`; keep CLI signing as a packaging step.
- Preserve local workspace/focus/display behavior documented in `.codex/skills/flashspace-local-release/references/local-behavior.md`.
